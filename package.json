{
  "private": true,
  "devDependencies": {
    "pagefind": "^1.4.0"
  },
  "scripts": {
    "build": "hugo && npx pagefind --site public"
  },
  "dependencies": {
    "Pagefind": "npm:pagefind@^1.4.0"
  }
}
